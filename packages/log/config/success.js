const headStyle = {
  bgColor: 'green',
  bold: true,
  italic: true,
}

const titleStyle = {
  color: 'green',
  bold: true,
}

const msgStyle = {}

module.exports = { headStyle, titleStyle, msgStyle }
