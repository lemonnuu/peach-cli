const headStyle = {
  bgColor: 'cyan',
  bold: true,
  italic: true,
}

const titleStyle = {
  color: 'cyan',
  bold: true,
}

const msgStyle = {}

module.exports = { headStyle, titleStyle, msgStyle }
