const headStyle = {
  bgColor: 'magenta',
  bold: true,
  italic: true,
}

const titleStyle = {
  color: 'magenta',
  bold: true,
}

const msgStyle = {}

module.exports = { headStyle, titleStyle, msgStyle }
